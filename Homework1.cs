namespace Homework1;

class Program 
{
    static void Main(string[] args) 
    {
        double X = 2.5; //Set the value of X and defining the variable as a double.
        double Y = 3.3; //Set the value of Y and defining the variable as a double.
        double Z = 4 * X * X + 3 * Y; //Set the Value of Z. Set 4x2 + 3Y as 4*X*X + 3*Y.
        Console.WriteLine($"The value of Z is: "+Z); //Print the value of z by placing $ symbol before the string so that Z can be placed in the ().
    }

}
